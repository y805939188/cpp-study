import React from 'react';
import { mount } from 'enzyme';
import Home from '..';
import { MemoryRouter, Route, Switch } from 'react-router';
const initState = {
  user: { userInfo: { id: 1, username: 'test' } },
}

test('测试Home路由', () => {

});
