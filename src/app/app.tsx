import React from 'react';
import ReactDOM from 'react-dom';
import Home from '../components/entry';
import 'antd/dist/antd.css';

ReactDOM.render(
  <div id="pd-operator-hub-wrapper">
    <Home />
  </div>,
  document.querySelector('#app'),
);
